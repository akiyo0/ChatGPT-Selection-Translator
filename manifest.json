{
  "manifest_version": 3,
  "name": "ChatGPT Selection Translator",
  "version": "1.0.0",
  "description": "Select text on any webpage or PDF, then translate it via the right-click menu, a keyboard shortcut, or the toolbar button, powered by the OpenAI API.",
  "permissions": ["contextMenus", "storage", "scripting", "activeTab"],
  "host_permissions": ["https://api.openai.com/*"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_title": "Translate selection (default target language)"
  },
  "options_page": "options.html",
  "commands": {
    "translate-selection": {
      "suggested_key": {
        "default": "Ctrl+Shift+Y",
        "mac": "Command+Shift+Y"
      },
      "description": "Quickly translate the current selection using the default target language"
    }
  },
  "content_scripts": [
    {
      "matches": ["http://*/*", "https://*/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
